<!-- <ion-grid>
  <ion-row>
    <ion-col size="12" lines="full">
      <div class="stores__item" *ngIf="categories">
        <div class="stores__item--image-overlay">
          <ion-img
            [src]="categories.imageUrl ? categories.imageUrl : 'https://ionicframework.com/docs/img/demos/thumbnail.svg'"
            [alt]="categories.name"
            [style.object-fit]="'cover'"
          ></ion-img>
          <div
            class="stores__item--actions"
            *ngIf="isAdmin"
            (click)="$event.stopPropagation()"
          >
          <p>{{ categories.name | titlecase }}</p>

            <ion-button
              size="small"
              mode="ios"
              fill="outline"
              color="primary"
              (click)="openUpdateModal(categories)"
            >
              Edit
            </ion-button>
            <ion-button
              mode="ios"
              (click)="onDelete(categories._id)"
              size="small"
              color="danger"
              fill="outline"
            >
              Delete
            </ion-button>
          </div>
        </div>
      </div>
    </ion-col>
  </ion-row>
</ion-grid> -->
<!-- .stores__item {
  position: relative;
}

.stores__item--image-overlay {
  position: relative;
}

.stores__item--image-overlay ion-img {
  display: block;
  width: 100%;
  height: auto;
}

.stores__item--actions {
  position: absolute;
  bottom: 0;
  left: 0;
  right: 0;
  background-color: rgba(245, 238, 238, 0.884);
  padding: 8px;
  display: flex;
  justify-content: space-around;
  align-items: center;
  color: white;
}

.stores__item--actions ion-button {
  font-size: 0.8em;
  
}
p{
 color: rgb(12, 12, 34); 
//  background-color:rgb(182, 185, 185);
 font-size: medium;
 font-style: oblique;
} -->
